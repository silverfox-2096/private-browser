# Hadolint config. Each ignore is a documented, deliberate choice -- not an oversight.
# The rationale lives in README.md ("Optional hardening") and Dockerfile.firefox comments.
ignored:
  # apt-get install without version pins, in the throwaway key-verification stage
  # only. Pinning curl/gnupg there would hold back their security updates and buys
  # nothing: the stage is discarded and its one output is checked by fingerprint.
  - DL3008
