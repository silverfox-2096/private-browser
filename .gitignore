# Secrets -- never commit
.env
webauth-htpasswd

# Runtime state and data (created at run time)
gluetun/

# Self-hosted CreepJS build output (optional 'test' profile)
creepjs/

# CI fingerprint job (ci/fingerprint.sh): downloaded CreepJS page + results
ci/creepjs-docs/
ci/out/
